name: celechron
description: Time scheduler for zjuers.
publish_to: "none"
version: 1.3.6+7
environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  get: ^4.7.3
  cupertino_icons: ^1.0.8 # CupertinoIcons 图标字体，勿删
  table_calendar: ^3.2.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.6.0
  extended_sliver: ^2.1.3
  url_launcher: ^6.3.2
  quiver: ^3.2.1
  workmanager:
    git:
      url: https://github.com/Celechron/flutter_workmanager.git
      ref: d7e6ba5ef3796fafe54d9739e481f01058751b4d # 固定 commit，避免 fork 的 main 分支变动导致构建漂移
      path: workmanager
  flutter_local_notifications: ^17.2.4
  flutter_secure_storage: ^9.2.4
  crypto: ^3.0.7
  path_provider: ^2.1.6
  share_plus: ^12.0.1 # 9fb0fb 提交当中提升了版本
  device_calendar: ^4.3.3
  timezone: ^0.9.4
  device_info_plus: ^12.4.0
  package_info_plus: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  # 仅构建期使用的工具（生成图标/改应用名），不参与运行时
  flutter_launcher_icons: ^0.14.4
  rename_app: ^1.6.6

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

flutter_launcher_icons:
  image_path: assets/logo.png
  android: true
