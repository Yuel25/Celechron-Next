## 界面与交互

- 成绩详情：学年学分/均绩/四分制及各项已选/未选统计卡片背景由深色强调色改为对应浅色容器令牌，保留原强调色作为点缀，修复深底深字与低对比度问题。
- 加权绩点：加权学分与加权五分制卡片同步采用浅色底与强调色点缀方案。
- 学期选择器：成绩详情、加权绩点、课程列表、考试列表页面的学期选择卡片与按钮选中态背景统一采用浅色容器色（`seasonWinter`），亮暗两种模式下可读性显著提升，满足 WCAG AA 大字对比度标准。

## 视觉令牌

- 容器令牌：`AppVisual.seasonWinter` 声明类型提升为 `CupertinoDynamicColor`，与 `AnimateButton` 等组件动态色参数严格对齐。

## 验证

- `flutter analyze` 通过（零警告、零错误）。
- 153 项自动测试全部通过。
