## 界面与交互

- 暗色模式：统一解析 `CupertinoDynamicColor`，修复辅助文字发灰/不可读；品牌底选中字与阴影适配暗色。
- 日历：今天改为品牌描边环，选中改为实心品牌色，状态更易区分。
- 课表：课程名/地点/教师字号与内边距提升，更早显示次要信息。
- 规划：顶部 grabber；成功后轻触感并约 0.9 秒自动关闭面板。
- 任务：完成/取消完成增加轻触感；进度条去 Material 依赖。
- 设置：分组 header/footer 统一使用 secondaryLabel。

## 架构

- `EverythingResult` 命名结果类替换 `Tuple6`。
- Scholar 拆为数据模型、同步编排（`ScholarSyncService`）、序列化。
- 成绩归类 `gradeGroupKey` / `groupGradesByCourseKey` 可测并补单测。
- `isGrs` 空安全；`print` 收口到 `DiagnosticLogService`。
- `SubtitleRow` 接入 `AppVisual` 排版令牌。

## 验证

- `flutter analyze` 通过。
- 153 项自动测试通过。
- 本地 release APK 已产出，待真机验收（暗色 / 日历 / 规划 / 课表）。
