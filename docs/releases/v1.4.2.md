## 修复

- 进度条填充渲染修复：修复自 v1.3.8 进度条去 Material 化后填充未能真正渲染的结构性问题。此前由于内部容器未指定高度因子，在松散约束下进度填充高度塌缩为 0，导致课程卡片、考试卡片与任务卡片上的进度条只剩下淡色背景轨道、填充色彩从未绘制。现已补齐高度约束并新增渲染尺寸回归测试，课程、考试与任务卡片上的进度条现在真正清晰可见，并随时间流逝与任务进展实时走动。

## 验证

- `flutter analyze` 静态分析通过（零警告、零错误）。
- 239 项自动化测试全部通过。
