## Celechron 1.3.9

本版本修复学业相关页面统计卡片与选择器的颜色对比度问题，提升亮暗两色模式下的文本可读性。

### 🎨 界面与对比度修复

- 成绩详情与加权绩点：统计卡片背景从深色强调色改为浅色容器色（`modulePractice`、`moduleHomework` 等），搭配原强调色点缀，彻底解决深底深字问题
- 学期选择器：成绩详情、加权绩点、课程列表、考试列表选中态背景统一改为浅色容器令牌（`seasonWinter`）
- 对比度提升：亮色与暗色模式下各卡片数值对比度均满足 WCAG AA 大文字可读性规范

### 🧩 令牌优化

- `AppVisual.seasonWinter` 声明类型提升为 `CupertinoDynamicColor`，规范组件参数类型兼容

### ✅ 验证

- 静态分析通过，153 项自动测试通过

**Android 9.0（API 28）及以上可用。**
